{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "gunicorn movieflix.wsgi:application --workers 1",
    "preDeployCommand": "python manage.py collectstatic --no-input && python manage.py migrate --no-input"
  }
}
